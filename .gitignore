node_modules/
dist/
*.log
.claude/guardian/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Staged plugin payload — built by `npm run stage-plugin`, never edited by hand.
build/
